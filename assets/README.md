# Model Assets

The page currently loads this GLB file from this folder:

- `model2.glb` final model, copied from `D:\大二课程资料\建筑与城市设计\虚拟模型\1.glb`
- `mobile.glb` lightweight model, loaded automatically on mobile/tablet clients

Old test versions are not used by the page.
